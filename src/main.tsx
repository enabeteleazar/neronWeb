import React from 'react';
import ReactDOM from 'react-dom/client';
import { NeronConsole } from './NeronConsole';
import './styles/global.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <>
    <NeronConsole />
  </>,
);
